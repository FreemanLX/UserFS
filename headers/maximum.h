#ifndef MAXIMUM_INCLUDED
#define MAXIMUM_INCLUDED
#define forp(i,size) for(int i=0; i<size; i++)
#define form(i,size) for(int i=size; i>=0; i--)
#include <sqlite3.h>
#include <termcap.h>
#include <bits/stdc++.h>
#include <iostream>
#include <cstring>
#include <fstream>
#include <sstream>
#include "structures.h"

using namespace std;

#endif // MAXIMUM_INCLUDED
#define FOR(TYPE, IDENT, X , BEGIN, END) for(TYPE IDENT = BEGIN, IDENT##_end = static_cast<decltype(IDENT)>(END); IDENT < IDENT##_end; IDENT+=X)
